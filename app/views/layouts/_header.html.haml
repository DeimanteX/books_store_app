%nav.navbar.navbar-toggleable-md.navbar-inverse.bg-primary
  - if user_signed_in?
    %u.pull-sm-right= link_to "Your Store", author_books_path(:author_id => current_user.id), :class => "text-white"
  %ul.nav.navbar-nav.navbar-logo.mx-auto
    %li
      = link_to image_tag("book-store-logo.png", alt: "Books Store Logo"), root_path
    %li
      %h1= link_to "Books Store", root_path, :class => "text-white"
  %ul.nav.navbar-nav.pull-sm-right
    - if user_signed_in?
      %li
        = link_to current_user.email + ' x', destroy_user_session_path, method: :delete, :class => "nav-link text-white",
          data: { confirm: 'Do you really want to log out?' }
    - else
      %li= link_to "Авторизация", new_user_session_path, :class => "nav-link"
      = link_to "Регистрация", new_user_registration_path, :class => "btn btn-outline-secondary my-2 my-sm-0"